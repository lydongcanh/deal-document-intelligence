"""Stage 3 — Structure.  [BUY + own adapter]

Blocks, headings, tables, pages and offsets. The parser produces these; the
*adapter* into our CanonicalDocument (with stable char offsets for evidence)
is the part we own carefully — evidence integrity depends on it.
"""
