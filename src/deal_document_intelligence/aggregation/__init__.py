"""Stage 9 — Aggregation into document & deal intelligence.  [BUILD]

Per-document intelligence is the `EvidenceBackedResult` the Pipeline assembles
(stage 9a). This package covers stage 9b: aggregating many documents into
`DealIntelligence`, including cross-document entity/alias resolution — the hard,
deal-defining problem.
"""
